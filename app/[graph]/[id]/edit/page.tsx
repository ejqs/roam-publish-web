/**
 * Edit UI stub — not part of the prototype slice.
 * Publishing is done from the Roam extension via API.
 */
export default function EditStubPage() {
	return (
		<main className="mx-auto flex min-h-svh max-w-lg flex-col justify-center px-6 py-10 text-zinc-700">
			<h1 className="text-xl font-semibold text-zinc-900">Edit</h1>
			<p className="mt-2 text-sm">
				Web edit UI is not included in this prototype. Publish from the Roam
				extension instead.
			</p>
		</main>
	);
}
